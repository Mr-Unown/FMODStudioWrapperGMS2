/// @description Setup platforms variables.
depth = 5;
image_speed = 0.35;

hsp = 0;
vsp = 0;

stopped = false;
stopid = noone;

//Smooth in and Out
stopSpeed = 0;
stopSpeedMax = min(4, moveSpeed);
stopSpeedAcc = 0.5;
stopSpeedDecc = 0.5;
platformDirection = targetDirection;

trueX = x;
trueY = y;



















